---
import { siteData } from "@/content/siteData";

interface Props {
  compact?: boolean;
}

const { compact = false } = Astro.props;
---

<aside class={`border-l-2 border-gold bg-bone/70 ${compact ? "p-5" : "p-6 md:p-8"}`} aria-label="Informacja prawna">
  <h2 class="font-serif text-2xl text-ink">Ważna informacja</h2>
  <p class="mt-3 text-sm leading-7 text-graphite/78">{siteData.disclaimer}</p>
  <p class="mt-3 text-sm leading-7 text-graphite/78">
    {siteData.disclaimerNote}
  </p>
</aside>
